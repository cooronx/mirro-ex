[db]
url = "http://127.0.0.1:8123"
user = "rust_user"
password = "123456"
database = "default"
pool_size = 5

[db.tables]
sh_order = "SHOrder"
sz_order = "SZOrder"
transaction = "Transaction"

[db.schema]
market_schema_path = "scripts/create_local_clickhouse_tables.sql"
trading_db_path = "data/trading.db"
trading_schema_path = "scripts/create_trading_sqlite_schema.sql"

[replay]
# 每个channel提前拉取多少个批次
lane_queue_capacity = 8
# 并行维护盘口和导出快照的worker数量
orderbook_workers = 6
# 主循环每隔多少毫秒推进一次 replay
tick_interval_ms = 5
# 输出盘口的档位深度
snapshot_depth = 10
# 是否导出 snapshot parquet
write_snapshot_parquet = true
# snapshot parquet 输出目录，每个交易日、每个标的一个文件
snapshot_parquet_dir = "data/order_book_snapshot"
# 每次从数据库拉取多少条事件，越小越省内存，但查询次数会更多
batch_size = 100000

[nats]
url = "nats://127.0.0.1:4222"
subject = "market.snapshot"

[web]
host = "127.0.0.1"
port = 5800

[logging]
level = "info"
directory = "logs"
file_prefix = "mirro-ex"
to_stdout = true
to_file = true
